<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/bluetooth"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".bluetoothDialog">


    <TextView
        android:id="@+id/pairedDeviceText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="등록된 기기"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"/>

    <ListView
        android:id="@+id/pairedDeviceList"
        android:layout_width="wrap_content"
        android:layout_height="340dp"
        app:layout_constraintTop_toBottomOf="@+id/pairedDeviceText"
        app:layout_constraintLeft_toLeftOf="parent" />

    <TextView
        android:id="@+id/scanDeviceText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="검색된 기기"
        app:layout_constraintTop_toBottomOf="@+id/pairedDeviceList"
        app:layout_constraintLeft_toLeftOf="parent" />

    <ListView
        android:id="@+id/scanDeviceList"
        android:layout_width="wrap_content"
        android:layout_height="340dp"
        app:layout_constraintTop_toBottomOf="@+id/scanDeviceText"
        app:layout_constraintLeft_toLeftOf="parent" />




</androidx.constraintlayout.widget.ConstraintLayout>